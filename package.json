{
  "name": "nft_art_generator",
  "version": "1.0.0",
  "description": "NFT art generator built off Hashlips art engine creates multiple artworks base on the layers of each variation.",
  "main": "index.js",
  "bin": "index.js",
  "pkg": {
    "assets": [
      "layers/**/*"
    ],
    "scripts": [
      "src/main.js",
      "src/config.js"
    ],
    "outputPath": "dist"
  },
  "scripts": {
    "build": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Confidence Nathaniel Yobo",
  "license": "MIT",
  "dependencies": {
    "all": "^0.0.0",
    "canvas": "^2.8.0",
    "sha1": "^1.1.1"
  }
}
